# Firebase React Auth App

Simple React app with Firebase Google login. After login, the user's name is shown on the right side of the top menu.

## Run locally

1. Copy `.env.example` to `.env`
2. Add your Firebase project credentials
3. Enable Google sign-in in your Firebase Console
4. Install dependencies with `npm install`
5. Start the app with `npm run dev`
